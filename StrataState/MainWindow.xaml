<Window x:Class="StarataState.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StrataState" Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        FontFamily="Segoe UI, Inter, Arial">

    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#FAFAFA"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#111111"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#666666"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#EAEAEA"/>
        <SolidColorBrush x:Key="InputBgBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0070F3"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#0F000000"/>
        <SolidColorBrush x:Key="SelectionBrush" Color="#1E000000"/>
    </Window.Resources>
    
    <Window.TaskbarItemInfo>
        <TaskbarItemInfo/>
    </Window.TaskbarItemInfo>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="6" GlassFrameThickness="-1" CornerRadius="8"/>
    </WindowChrome.WindowChrome>

    <Grid x:Name="RootGrid" Background="{DynamicResource BgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="Transparent">
            <TextBlock Text="StrataState - PDA Simulator" VerticalAlignment="Center" Margin="15,0,0,0" 
                   FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" 
                   IsHitTestVisible="False"/>

        </Grid>

        <StackPanel Grid.Row="1" Margin="20,10,20,20">
            <TextBlock Text="Pushdown Automaton Simulator" FontSize="24" FontWeight="Bold" 
                       Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"/>
            <TextBlock Text="L = { a^n b^n | n ≥ 1 }" FontSize="14" Foreground="{DynamicResource MutedTextBrush}" Margin="0,0,0,20"/>

            <Border Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" 
                    BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="InputTextBox" Grid.Column="0" Height="36" Padding="10,0" 
                             VerticalContentAlignment="Center" FontSize="14"
                             Background="{DynamicResource InputBgBrush}" Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>

                    <Button Grid.Column="1" Content="Check" Width="100" Margin="10,0,0,0"
                            Background="{DynamicResource PrimaryBrush}" Foreground="White" 
                            BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"
                            Click="BtnCheck_Click"/>
                </Grid>
            </Border>

            <Border x:Name="ResultBorder" CornerRadius="6" Padding="12" Visibility="Collapsed">
                <TextBlock x:Name="ResultTextBlock" FontSize="14" FontWeight="Medium" HorizontalAlignment="Center"/>
            </Border>
        </StackPanel>

        <Border Grid.Row="2" Margin="20,0,20,20" Background="{DynamicResource CardBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
            <DataGrid x:Name="TraceDataGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                      Background="Transparent" RowBackground="Transparent" HeadersVisibility="Column"
                      BorderThickness="0" GridLinesVisibility="Horizontal" 
                      HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                      SelectionMode="Single" CanUserAddRows="False" CanUserResizeRows="False">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>

                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SelectionBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Step" Binding="{Binding StepNumber}" Width="60"/>
                    <DataGridTextColumn Header="State" Binding="{Binding CurrentState}" Width="80"/>
                    <DataGridTextColumn Header="Input" Binding="{Binding UnreadInput}" Width="120"/>
                    <DataGridTextColumn Header="Stack" Binding="{Binding StackContent}" Width="120"/>
                    <DataGridTextColumn Header="Action" Binding="{Binding ActionDescription}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <StackPanel Grid.Row="3" VerticalAlignment="Bottom" Margin="0,5,0,15">
            <TextBlock x:Name="FooterTeam" 
                       Text="StrataState v.1.0.0 © 2026 | Engineered by Team 14" 
                       HorizontalAlignment="Center" 
                       FontSize="12" 
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextBrush}"
                       Margin="0,0,0,4"/>

            <TextBlock x:Name="FooterMembers" 
                       Text="Raziq Danish Safaraz • Danish Faiq Ibad Yuadi • Rafa Huga Nirando" 
                       HorizontalAlignment="Center" 
                       FontSize="11" 
                       FontWeight="Medium"
                       Foreground="{DynamicResource MutedTextBrush}"/>
        </StackPanel>
    </Grid>
</Window>